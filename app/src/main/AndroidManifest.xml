<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.examples.smikalcsanad.tengernyitudas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tengernyi tudás"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:screenOrientation="landscape">
            //android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>
        <activity android:name=".ChapterSelectionActivity"
            android:screenOrientation="landscape">

        </activity>

        <activity android:name=".ChapterOneActivity"
            android:screenOrientation="landscape">

        </activity>

        <!-- Quiz activities -->
        <activity android:name=".QuizSelectionActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".FirstQuizActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".SecondQuizActivity"
            android:screenOrientation="landscape"/>
    </application>

</manifest>